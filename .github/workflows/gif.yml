name: Celebratory GIF

on:
  issues:
    types:
      - closed

jobs:
  celebrate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Comment with celebratory GIF
        uses: actions/github-script@v4
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const ctx = github.context;

            if (context.eventName === 'pull_request') {
              const commentBody = `🎉 Congratulations on merging this pull request! Enjoy this celebratory GIF: ![Celebration](${gifUrl})`;
              github.issues.createComment({
                issue_number: ctx.issue.number,
                owner: ctx.repo.owner,
                repo: ctx.repo.repo,
                body: commentBody
              });
            } else if (context.eventName === 'issues') {
              const commentBody = `🎉 This issue has been closed! Enjoy this celebratory GIF: ![Celebration](${gifUrl})`;
              github.issues.createComment({
                issue_number: ctx.issue.number,
                owner: ctx.repo.owner,
                repo: ctx.repo.repo,
                body: commentBody
              });
            }

